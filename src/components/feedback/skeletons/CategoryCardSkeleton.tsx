import Skeleton from "react-loading-skeleton";

const CategoryCardSkeleton = () => {
  return <Skeleton width={300} height={310} style={{ borderRadius: "10px" }} />;
};

export default CategoryCardSkeleton;
