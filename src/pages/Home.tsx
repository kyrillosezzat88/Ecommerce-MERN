import { HeroSlider } from "@components/common";
import { ProductsTabs } from "@components/ecommerce";

const Home = () => {
  return (
    <section>
      <HeroSlider />
      <ProductsTabs />
    </section>
  );
};

export default Home;
